name: Production Deploy

on:
  push:
    branches:
      - master

jobs:
  # ФРОНТЕНД
  # deploy-frontend:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4

  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: '18'
  #         cache: 'npm'
  #         cache-dependency-path: frontend/package-lock.json

  #     - name: Install & Build Frontend
  #       run: |
  #         cd frontend
  #         npm ci
  #         npm run build
  #       # TODO: Увага: перевір, куди білдить твій проект (dist або build). 
  #       # TODO: Vite зазвичай робить 'dist', Create-React-App робить 'build'.
      
  #     - name: Copy files to Server
  #       uses: appleboy/scp-action@master
  #       with:
  #         host: ${{ secrets.SSH_HOST }}
  #         username: ${{ secrets.SSH_USER }}
  #         key: ${{ secrets.SSH_KEY }}
  #         # TODO: Копіюємо вміст папки dist (або build) на сервер
  #         source: "frontend/dist/*"
  #         target: "/var/www/demo.harkushyn.com"
  #         strip_components: 2


  # БЕКЕНД
  deploy-backend:
    runs-on: ubuntu-latest
    needs: deploy-frontend
    steps:
      - name: SSH and Deploy Backend
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd /opt/anon-confess
            git pull origin master
            docker compose up -d --build backend
            docker image prune -f
